/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com']
    }
    // unknown source se images ko domains mai specify karna padega
};

export default nextConfig;
